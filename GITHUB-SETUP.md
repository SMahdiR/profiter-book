# 🚀 راهنمای آپلود روی GitHub Pages

این راهنما به شما کمک می‌کند تا کتابچه Profiter را روی GitHub Pages قرار دهید و از طریق اینترنت در دسترس قرار دهید.

## 📋 فایل‌های مورد نیاز برای آپلود

فقط این فایل‌ها را روی GitHub آپلود کنید:

### ✅ فایل‌های ضروری:
- `index.html` - فایل اصلی PWA
- `manifest.json` - تنظیمات PWA
- `service-worker.js` - برای کار آفلاین

### 📚 فایل‌های راهنما (اختیاری):
- `INSTALL-GUIDE.html` - راهنمای نصب بصری
- `README.md` - راهنمای پروژه

### ⚠️ فایل‌های غیرضروری (نیازی به آپلود نیست):
- فایل‌های `.txt` (فصل‌ها و پیشگفتار)
- فایل‌های `.py` (اسکریپت‌های تولید)
- فایل‌های HTML جداگانه (chapter-*.html, preface.html و...)

---

## 📝 مراحل آپلود روی GitHub

### روش 1: استفاده از GitHub Web Interface (ساده‌ترین)

#### مرحله 1: Repository شما آماده است! ✅
Repository شما در آدرس زیر ایجاد شده است:
- **Repository:** [https://github.com/SMahdiR/profiter-book](https://github.com/SMahdiR/profiter-book)

حالا فقط باید فایل‌ها را آپلود کنید.

#### مرحله 2: آپلود فایل‌ها
1. در صفحه repository جدید، روی **"uploading an existing file"** کلیک کنید
2. فایل‌های زیر را drag & drop کنید یا انتخاب کنید:
   - `index.html`
   - `manifest.json`
   - `service-worker.js`
   - `INSTALL-GUIDE.html` (اختیاری)
   - `README.md` (اختیاری)
3. در پایین صفحه، یک commit message بنویسید (مثلاً: "Initial commit - Profiter PWA")
4. روی **"Commit changes"** کلیک کنید

#### مرحله 3: فعال‌سازی GitHub Pages
1. در repository، به **Settings** بروید
2. در منوی سمت چپ، **"Pages"** را انتخاب کنید
3. در بخش **"Source"**:
   - **Branch:** `main` (یا `master`)
   - **Folder:** `/ (root)`
4. روی **"Save"** کلیک کنید
5. ⏳ چند دقیقه صبر کنید تا GitHub Pages فعال شود
6. آدرس سایت شما: **`https://SMahdiR.github.io/profiter-book`**

---

### روش 2: استفاده از Git Command Line

اگر Git روی کامپیوتر شما نصب است:

```bash
# 1. ایجاد repository محلی
git init

# 2. اضافه کردن فایل‌ها
git add index.html manifest.json service-worker.js INSTALL-GUIDE.html README.md

# 3. Commit
git commit -m "Initial commit - Profiter PWA"

# 4. اتصال به GitHub (بعد از ایجاد repository در GitHub)
git remote add origin https://github.com/SMahdiR/profiter-book.git

# 5. Push
git branch -M main
git push -u origin main
```

سپس مراحل **"فعال‌سازی GitHub Pages"** را از روش 1 انجام دهید.

---

## 🔗 دریافت لینک نهایی

بعد از فعال‌سازی GitHub Pages، لینک شما:

```
https://SMahdiR.github.io/profiter-book
```

🔗 **لینک Repository:** [https://github.com/SMahdiR/profiter-book](https://github.com/SMahdiR/profiter-book)

---

## ✅ تست نصب PWA

1. لینک GitHub Pages را در مرورگر موبایل باز کنید
2. منوی مرورگر → **"Add to Home screen"**
3. ✅ باید به درستی نصب شود!

---

## 🔧 تنظیمات اضافی (اختیاری)

### تغییر نام Repository
اگر می‌خواهید نام repository را تغییر دهید:
1. Settings → General → Repository name
2. نام جدید را وارد کنید
3. لینک GitHub Pages به‌طور خودکار به‌روزرسانی می‌شود

### اضافه کردن آیکون
اگر می‌خواهید آیکون‌های سفارشی اضافه کنید:
1. فایل‌های `icon-192.png` و `icon-512.png` را ایجاد کنید
2. در repository آپلود کنید
3. فایل `manifest.json` به‌طور خودکار از آن‌ها استفاده می‌کند

---

## ⚠️ نکات مهم

1. **HTTPS:** GitHub Pages به‌طور خودکار HTTPS را فعال می‌کند (ضروری برای PWA)
2. **Service Worker:** فقط روی HTTPS یا localhost کار می‌کند
3. **Cache:** بعد از هر تغییر، ممکن است نیاز به پاک کردن cache مرورگر باشد
4. **بروزرسانی:** بعد از هر push به GitHub، تغییرات در عرض چند دقیقه اعمال می‌شود

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. مطمئن شوید repository **Public** است
2. GitHub Pages در Settings → Pages فعال باشد
3. فایل `index.html` در root repository باشد
4. چند دقیقه صبر کنید تا تغییرات اعمال شود

---

**موفق باشید! 🎉**

