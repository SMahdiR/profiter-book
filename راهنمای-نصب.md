# 📱 راهنمای نصب کتابچه Profiter روی گوشی

## 🎯 روش‌های نصب

### روش 1: نصب مستقیم (ساده‌ترین)

#### برای اندروید:
1. فایل `index.html` را در مرورگر Chrome یا Edge باز کنید
2. منوی مرورگر (سه نقطه ☰) را باز کنید
3. گزینه **"Add to Home screen"** یا **"نصب اپلیکیشن"** را انتخاب کنید
4. روی **"Install"** یا **"نصب"** کلیک کنید
5. ✅ آیکون روی صفحه اصلی گوشی شما اضافه می‌شود!

#### برای iOS (آیفون/آیپد):
1. فایل `index.html` را در مرورگر **Safari** باز کنید
2. دکمه **Share** (مربع با فلش ⬆️) را بزنید
3. گزینه **"Add to Home Screen"** را انتخاب کنید
4. نام را ویرایش کنید (اختیاری)
5. روی **"Add"** کلیک کنید
6. ✅ آیکون روی صفحه اصلی شما اضافه می‌شود!

---

### روش 2: استفاده از سرور (برای Service Worker)

اگر می‌خواهید قابلیت آفلاین کامل کار کند، باید از یک سرور استفاده کنید:

#### گزینه A: GitHub Pages (رایگان - توصیه می‌شود)
1. فایل‌ها را در یک repository در GitHub آپلود کنید
   - فایل‌های ضروری: `index.html`, `manifest.json`, `service-worker.js`
   - راهنمای کامل: فایل `GITHUB-SETUP.md` را مطالعه کنید
2. Settings → Pages → Source را روی `main` branch تنظیم کنید
3. لینک شما: **`https://SMahdiR.github.io/profiter-book`**
   - 🔗 Repository: [github.com/SMahdiR/profiter-book](https://github.com/SMahdiR/profiter-book)
4. لینک را در مرورگر موبایل باز کنید
5. سپس از منوی مرورگر "Add to Home screen" را انتخاب کنید

#### گزینه B: Netlify (رایگان)
1. به [netlify.com](https://netlify.com) بروید
2. فایل‌ها را drag & drop کنید
3. لینک ارائه شده را در مرورگر موبایل باز کنید
4. سپس "Add to Home screen" را انتخاب کنید

#### گزینه C: سرور محلی (برای تست)
```bash
# Python
python -m http.server 8000

# Node.js
npx http-server

# سپس در مرورگر موبایل:
# http://IP-ADDRESS:8000
```

---

## ⚠️ نکات مهم

1. **Service Worker**: برای کار آفلاین، باید از یک سرور استفاده شود (نه فایل مستقیم)
2. **HTTPS**: در تولید، باید از HTTPS استفاده شود
3. **Manifest**: فایل `manifest.json` باید در همان پوشه باشد
4. **Icons**: برای نمایش بهتر، آیکون‌های `icon-192.png` و `icon-512.png` را اضافه کنید

---

## 🔧 عیب‌یابی

### اگر "Add to Home screen" نمایش داده نمی‌شود:
- ✅ مطمئن شوید از HTTPS استفاده می‌کنید (یا localhost)
- ✅ فایل `manifest.json` موجود باشد
- ✅ مرورگر از PWA پشتیبانی کند (Chrome, Edge, Safari)

### اگر Service Worker کار نمی‌کند:
- ✅ از یک سرور استفاده کنید (نه فایل مستقیم)
- ✅ Console مرورگر را بررسی کنید
- ✅ Cache را پاک کنید و دوباره امتحان کنید

---

## 📞 پشتیبانی

اگر مشکلی داشتید، لطفاً بررسی کنید:
1. Console مرورگر برای خطاها
2. Application → Service Workers در DevTools
3. Application → Manifest برای بررسی manifest

---

**موفق باشید! 🎉**

